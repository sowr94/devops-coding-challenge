apiVersion: v1
kind: Service

metadata:
  name: crewmeister-service

spec:
  selector:
    app: crewmeister-app

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080

  type: {{ .Values.service.type }}
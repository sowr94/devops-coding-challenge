apiVersion: v1
kind: Service

metadata:
  name: crewmeister-service

spec:
  selector:
    app: crewmeister-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer